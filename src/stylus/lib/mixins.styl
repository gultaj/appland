spriteX($sprite, $offset)
  $a = round($sprite[2] + ($offset - $sprite[4]) / 2)
  return $a


spriteY($sprite, $offset)
  $a = round($sprite[3] + ($offset - $sprite[5]) / 2)
  return $a

spriteP($sprite, $offset = 0) {
  background-position: spriteX($sprite, $offset) spriteY($sprite, $offset)
}
